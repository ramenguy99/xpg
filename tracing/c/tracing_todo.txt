Maintenance:
[-] Fix include from multiple files
[ ] Write unit tests

Perf:
[ ] Assign global integer index to each tracepoint, use this for direct indexing (into table list)
[ ] Do something similar for runtime tracepoint count (assume all tracepoints statically known)

TCP:
[-] Write cross platform sendv wrapper to send tcp messages in a single call
[ ] Unsubscribe from all tracepoints while tcp disconnected
[ ] Use non-blocking tcp connect
[ ] Add tcp reader thread, use atomics to enable / disable tracepoints at runtime

SQLite:
[ ] No unnecessary string formatting for sqlite sub
[ ] Fallback to dynamic allocation for table creation if does not fit in stack-allocated buffer (snprintf helper, or sqlite helpers)
[ ] No table verification (just use sqlite internal verification?)

Robustness:
[x] Global max ndim and ensure that tracing fails gracefully if not respected at runtime
    -> maybe even one macro per dim